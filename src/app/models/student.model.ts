export class Students {
  public id: string;
  public name: string;
  public dob: string;

  constructor(id: string, name: string, dob: string) {
    this.id = id;
    this.name = name;
    this.dob = dob;
  }
}
